# CARpsy — Python dependencies
#
# Python 3.11+ required (install from https://www.python.org/downloads/)
# DO NOT install from the Microsoft Store — that version is a stub (0 bytes).
#
# Install:  pip install -r requirements.txt

# Configuration parsing
pyyaml>=6.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# DTC database download
gitpython>=3.1.40
requests>=2.31.0

# Evaluation metrics (step 5)
scikit-learn>=1.3.0

# ── Notes ──────────────────────────────────────────────────────────────────
# python-dotenv is NOT needed — .env is parsed inline in each script.
#
# QVAC Fabric is a LOCAL BINARY (llama.cpp fork), not a pip package.
# Compile it from: https://github.com/tetherto/qvac-fabric-llm.cpp
